import React, { useState, useEffect } from "react";
import { 
  Calendar, User, Mail, Phone, Package, MapPin, 
  DollarSign, Clock, Save, X, AlertCircle, CheckCircle,
  Search, Filter, Eye
} from 'lucide-react';
import NavBar from '../components/NavBar'


export default function CreateBooking() {
 
  return (
    <div>
      <NavBar/>
    </div>
  );
}